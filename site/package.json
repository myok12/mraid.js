{
  "name": "mraid-site",
  "description": "a website using mraid.js",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile-lib": "INPUT=lib-coffee ; OUTPUT=lib ; rm -rf $OUTPUT; cp -r $INPUT $OUTPUT ; find $OUTPUT -name '*.coffee' -exec rm {} \\; ; node_modules/coffee-script/bin/coffee -c -o $OUTPUT $INPUT",
    "start": "http://localhost:8000 && python -m SimpleHTTPServer",
  },
  "dependencies": {
    "coffee-script": "*",
  },
  "devDependencies": {
  },
  "engines": {
    "node": "*"
  }
}
